import { Head } from "../../../head.ts";

<Head
	title="Docs — Sfotty Pie"
	description="Documentation for Sfotty Pie, a browser-based Atari 8-bit emulator and 6502 toolkit."
/>

# Docs

This page is authored in **MDX** and compiled to a Preact component by
`@mdx-js/rollup`, then dropped into the route table like any other page.

Its `<title>` and social-card tags come from the `<Head>` component at the top —
the same `useHead` the `.tsx` pages use, collected into the static `<head>` at
build time.

- Plain Markdown prose works.
- So does inline JSX, when a doc needs it.

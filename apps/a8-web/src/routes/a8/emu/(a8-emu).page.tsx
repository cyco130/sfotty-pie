// The bare emulator (/a8/emu): no panel open. The persistent shell is the
// layout; this index route simply renders nothing in the sidebar slot.
export default function EmuIndexPage() {
	return null;
}
